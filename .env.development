# Local SFMB API origin used by the Vite /api proxy (not exposed to the browser).
DEV_API_ORIGIN=https://localhost:51592

# Leave empty so the app calls /api on the Vite origin and the proxy forwards it.
# For a remote API, set the origin with no trailing slash (CORS must allow this UI).
VITE_API_BASE_URL=
